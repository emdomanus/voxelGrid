# Luau-LSP definitions

Run the following once after cloning:

    .\scripts\luau-lsp\fetch-roblox-types.ps1

The downloaded Roblox definitions are intentionally ignored. The analyzer wrapper uses them
together with the Rojo sourcemap generated from dev.project.json.
