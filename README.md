# voxelGrid

Private Roblox package for a generic in-memory voxel-grid spatial index.

The initial package is a behavior-preserving extraction from VoxelMMO. Public API and
coordinate-range revisions are intentionally deferred to the isolated package review.
